# -*- mode: ruby -*-
# vi: set ft=ruby :

require 'yaml'

# -----------------------------------------------------------------------
# ROCKY LINUX 9 TESTING - Rancher deployment test
# -----------------------------------------------------------------------
# Usage:
#   vagrant up --vagrantfile=Vagrantfile.rocky
#   vagrant ssh --vagrantfile=Vagrantfile.rocky
#   vagrant destroy -f --vagrantfile=Vagrantfile.rocky
# -----------------------------------------------------------------------
begin
  test_vars = YAML.load_file('test_vars.yml')
  RANCHER_PASSWORD = test_vars['rancher_bootstrap_password']
rescue Errno::ENOENT
  RANCHER_PASSWORD = "TestPassword123!"
  puts "WARNING: test_vars.yml not found, using default password"
rescue => e
  RANCHER_PASSWORD = "TestPassword123!"
  puts "WARNING: Failed to load test_vars.yml: #{e.message}"
end

Vagrant.configure("2") do |config|
  # Rocky Linux 9 VM
  config.vm.box = "bento/rockylinux-9"
  config.vm.hostname = "rancher-rocky"
  config.vm.network "private_network", ip: "192.168.56.11"

  config.vm.provider "virtualbox" do |vb|
    vb.name = "rancher-rocky-9"
    vb.memory = "4096"
    vb.cpus = 2
    vb.linked_clone = true
  end

  config.vm.provision "ansible" do |ansible|
    ansible.playbook = "test_playbook.yml"
    ansible.verbose = "v"
    ansible.extra_vars = {
      rancher_bootstrap_password: RANCHER_PASSWORD,
      rancher_configure_firewall: false,
      ansible_python_interpreter: "/usr/bin/python3"
    }
  end
end
